#include <stdio.h>
#include <string.h>
#include <math.h>

#define max(a,b) (a>b?a:b)
#define min(a,b) (a<b?a:b)
#define abs(a) (a>0?a:-a)
#define swap(a,b) (a ^= b ^= a ^= b)


double hei();
double nsen(double x);

int main () {
    double xa = 0, xb = ;

    hei()




    return 0;
}

double nsen(double x){
    double f = (x^5) + 2*(x^3) + (-5)*x + 10;

    return f;
}

double hei(){
    double xa=0, xb=1;

while (1){
    
    double xa=nsen(xa);
    double xb=nsen(xb);
    double i;

    if(xa * xb >= 0){
        xa-=i;
        xb+=i;
    }

    return 
}

}